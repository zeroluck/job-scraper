name: Parse Resume Manually

on:
  workflow_dispatch:
    inputs:
      archetype:
        description: "Canonical lane, all enabled lanes, or global legacy resume"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - technology_delivery
          - systems_platform_ops
          - network_infrastructure
          - datacenter_operations
          - ai_workflow_automation
          - building_controls
          - global

concurrency:
  group: parse-base-resume
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  parse_resume_job:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            echo "requirements.txt not found. Please create one with necessary packages."
            # Optionally, install known dependencies directly if requirements.txt is missing
            # pip install pdfplumber google-generativeai supabase
            exit 1
          fi

      - name: Run resume parser script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          # LLM Configuration (set via Repository Variables / Secrets)
          LLM_API_KEY: ${{ secrets.LLM_API_KEY || secrets.GEMINI_FIRST_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          RESUME_PARSE_ARCHETYPE: ${{ inputs.archetype }}
        run: python resume_parser.py
